---
label: SEO
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
  description: Title of the page. This will appear as the title on the browser's tab
    and social previews.
- name: description
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Description
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
pages:
- content/global-seo.md
